[tracker]
url = "https://tracker.zz.hgdev.com"
project_id = 10
project = "vikunja-mcp"
# enforce_single_wip = true  # refuse claim() while you already have a Design/Build task
#   (single-WIP discipline). Off by default. Left off here: this repo's orchestrator +
#   a concurrent /loop share ONE token, so a hard gate would refuse the loop's claims.
